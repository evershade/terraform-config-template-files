
tfvars = "only works with TF >= 1.8"

tfvars_myvar     = "${myvar}"
tfvars_mylistvar = ${format("%#v", mylistvar)}

merge_key = {
  tfvars = "tfvars_val"
}
# merge_key = "not mergeable"  # This will trigger a non-mergeable error
