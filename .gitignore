# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*
*.terraform.lock.hcl

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sentitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars

# Include specific .tfvars files you do wish to add to version control using negated pattern
# !example.auto.tfvars
!*.incl.auto.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf
!*.incl.override.tf

# Ignore tfplan files to exclude the plan output of command: terraform plan -out=tfplan
# example: *tfplan*
*.tfplan
tfplan

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore Sentinel Terraform mock files
.sentinel_mocks

# Ignore directory for managing TFC via remote backend
**/.tfc_remote/*

# Ignore directory for using Terraform Console
**/.tf_console/*

# Ignore scratch dirs and files for temporary working files
**/.scratch/*
*.scratch.md
*.scratch

# Ignore test files for temporary testing files
.test.*

# OSX files
*.DS_Store
